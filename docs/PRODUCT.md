# CalendarMark 产品设计

## 1. 产品定位

CalendarMark 不是传统的任务日历，而是一张“时间地图”：用户在日期上留下轻量的标签、文字和图片，之后可以用月份视角重新看见自己的工作和生活轨迹。

核心体验是：**看到日期 → 快速标记 → 需要时补充上下文 → 回顾**。

## 2. MVP 用户故事

1. 我在 Windows 上按快捷键，就能把 CalendarMark 主窗口叫出来。
2. 我点击日历中的一天，右侧抽屉滑入，不需要离开当前月份。
3. 我可以输入标题、正文，点击快捷标签，也可以直接创建新标签。
4. 我可以添加图片、TXT、Markdown 或 PDF 作为当天的附件。
5. 我关闭窗口后应用仍在托盘，需要时可以从托盘重新打开或退出。
6. 我可以在设置中开启开机启动、录制快捷键、调整主题、管理标签和 Notion 数据源。
7. 我编辑已同步的记录时，已上传的附件不会被重复上传。
8. 我停用一个标签后，它不再出现在选择列表，但历史记录上的标签原样保留。
9. 我没有现成的 Notion 数据库时，可以直接在设置里选一个父页面并新建数据库。
10. 我删除一条日期记录时，应用会先让我确认，避免误触。
11. 我点击侧栏快捷入口的标签时，能看到带这个标签的日期列表，点某一天直接跳到那天的记录。
12. 我点击日历标题的年月，能快速跳到任意年月。
13. 我记录当天心情后，日历格子上会显示对应的表情。
14. 我点击日历的某一天，右侧（抽屉模式在下方）直接出现这天的编辑面板，不需要弹出抽屉。

## 3. 信息架构

```text
CalendarMark
├── 日历总览
│   ├── 月份切换
│   ├── 日期格子 / 标签 / 附件标记
│   ├── 本月概览
│   └── 最近更新
├── 日期记录抽屉
│   ├── 标题
│   ├── 内容
│   ├── 快捷标签
│   ├── 附件
│   └── 保存 / 删除
└── 设置
    ├── 数据源 / Notion / 本地存储 / 新建数据库
    ├── 系统 / 开机启动 + 快捷键
    ├── 界面 / 主题 + 窗口·抽屉模式
    └── 标签管理
```

## 4. 视觉和交互原则

- **像纸张一样安静**：米白画布、绿色主色、低对比边界，减少工具感。
- **信息密度可控**：日历格子只展示最多两个标签，其余用 `+N` 收起。
- **上下文不丢失**：日期编辑使用右侧抽屉，不跳转页面；抽屉关闭时保留月份和选中日期。
- **反馈要即时**：保存、删除、标签变更和 Notion 连接/同步结果均提供轻量 toast。
- **桌面优先、移动可用**：桌面使用侧边栏；窄屏时侧边栏收缩为图标，设置导航转为图标栏。

## 5. 关键交互

### 日期记录

- 点击任意日期打开抽屉。
- 没有记录时显示空白草稿；已有记录时加载该日期记录。
- 标题、正文、标签、附件任意一项有内容即可保存。
- 关闭按钮、遮罩、取消按钮都只关闭抽屉，不丢失已经保存的记录。
- 附件单个限制 5 MB；超过限制时不加入草稿。

### 托盘和快捷键

- Windows/macOS 启动后创建托盘图标。
- 托盘右键菜单包含：打开 CalendarMark、打开设置、退出。
- 窗口关闭事件被拦截并隐藏窗口，应用继续驻留托盘。
- 默认快捷键为 `CommandOrControl+Shift+Space`；设置改变后重新注册。

### 系统设置

- 设置页提供“系统”分区，包含开机启动和全局快捷键。
- 开机启动通过系统注册表 / Launch Agent 实现，仅桌面版可用；浏览器和移动端显示不可用状态。
- 快捷键通过“读取组合键”按钮录制：点击后直接按下目标组合键（至少包含 Ctrl/Cmd/Alt），Esc 取消，确认后立即生效，不再手工输入字符串。
- 日历总览工具栏在远程直连模式下提供刷新按钮，随时重新读取当前数据集。
- 全局快捷键支持 toggle 语义：窗口已聚焦时收起，未聚焦时带回焦点，隐藏时显示。
- 设置内容为单页滚动结构：左侧导航点击平滑滚动到对应分区，滚动时反向高亮；滚动条空间常驻，切换分区不会引起布局跳动。

### 界面模式

- **窗口模式**：常规桌面窗口，完整双栏布局。
- **抽屉模式**（桌面端专属）：无边框窄窗口贴屏幕右侧，顶部提供拖拽区和收起按钮；布局切换为紧凑顶栏形态，主区域在视口内滚动，窗口唤起时内容从右侧滑入。

### 标签

- 日历格子显示当日全部标签，数字贴近左上角。
- 删除标签 = 停用：不再出现在快捷标签、抽屉选择器和侧栏快捷入口，但已有记录、日历展示和 Notion `multi_select` 保持不变。
- 标签管理页分为“我的标签”和“已停用标签”两组，停用标签可一键恢复；重新创建同名标签会自动恢复而不是报错。
- 快捷入口的标签点击后在侧栏右侧弹出日期浮层（带滑入动效），列出最近 8 个日期与记录标题，点击即定位到该日；再次点击标签或选择日期后浮层关闭。

### 删除保护

- 记录抽屉中的删除按钮第一次点击只显示“删除这条记录？”确认条；确认后才执行本地删除或 Notion 归档，取消则回到编辑状态。

### 日历总览

- 顶栏侧栏不再显示“快速打开”提示块；“Notion 远程数据 / 本地数据”状态可点击，直接进入设置的数据源分区。
- 月份标题可点击，展开年月快捷选择器（年份翻页 + 12 个月网格）。
- 有记录的日期加粗高亮，若记录包含心情则在日期数字旁显示表情。
- 本月概览、最近更新、小提示卡片已移除；记录编辑面板常驻（窗口模式在日历右侧、抽屉模式在日历下方）。

### 心情

- 记录面板提供 12 个心情表情（可清除），保存时随记录存储；Notion 数据源会写入“心情”select 属性（按属性类型识别，缺失时忽略）。

### 设置

- Notion 连接引导、快捷键节奏提示、标签小建议等说明性卡片已移除；Integration Token 右侧提供“获取 Token”按钮，使用系统默认浏览器打开 Notion Integrations 页面。

### Notion

数据源设置不把 Notion 写死为唯一选项，而是使用可替换的数据源选择器。当前提供：

- **Notion**：首个已实现的外部数据源，选中即远程直连——读取、保存、删除直接作用于当前数据集；支持数据集发现、添加/移除本机同步目标、数据集切换、连接检查、手动重新读取、页面归档和附件上传。
- **本地存储**：当前可用，适合离线记录；可在设置中对已配置的 Notion 数据集执行“拉取到本地 / 推送到 Notion”。
- **本地存储卡片**：只列出本机已绑定的 Notion 数据集作为同步目标；Token、数据集发现和连接引导统一放在 Notion 数据源页，本地模式不显示连接引导。
- **WebDAV / Obsidian Vault**：展示为规划中的数据源，暂不允许连接。

选择 Notion 后，页面内提供三步连接引导：创建 Internal connection 并复制 token、将目标数据库通过 Add connections 分享给连接、回到 CalendarMark 发现并添加数据集。Token 不应被截图或提交到版本库。

数据集管理是本机配置，不会删除远端内容。用户可以点击“发现数据集”读取当前 Token 已授权的 data source，点击“添加数据集”保存多个同步目标，再点击列表项切换当前目标；移除数据集只移除本机引用。

建议映射如下：

| CalendarMark | Notion 属性 | 类型 |
| --- | --- | --- |
| `date` | Date | date |
| `title` | Title | title |
| `content` | Content | rich_text |
| `tagIds` | Tags | multi_select |
| `attachments` | Attachments | files / URL |

CalendarMark 不在网页层直连 Notion，而是通过 Tauri IPC 将操作交给 Rust 适配器，避免把请求逻辑和令牌放进浏览器网络层。发现数据集时按 Token 搜索可访问的 data source，选定后读取 schema，再按属性类型建立映射；至少需要一个 `title` 和一个 `date` 属性。

数据源语义分为两种：选择 Notion 时为远程直连，应用启动、切换数据集或点击“重新读取”时查询远端页面，日历展示当前 Notion 数据，保存和删除直接写入远端，不落盘到本机 entries/tags 存储；选择本地存储时以本机记录为主，用户可在设置中显式点击“拉取到本地”或“推送到 Notion”。

推送规则：没有 `remote` 引用的本地记录创建 Notion 页面，已有引用的记录更新对应页面；本地附件通过 Notion File Upload API 上传到 `files` 属性。删除带远端引用的本地记录时，CalendarMark 会先将 Notion 页面移入回收站。

## 6. 非目标（MVP 不做）

- 多人协作、账户系统、CalendarMark 云端服务。
- 复杂任务管理、提醒、重复事件和全天事件语义。
- 在本地存储层直接保存大量原图或视频。
- Notion 双向同步的自动冲突合并、后台队列和离线重试。

## 7. 验收清单

- [x] 月历显示当前月份和跨月占位日期。
- [x] 日期显示标签和附件状态。
- [x] 新建/编辑/删除日期记录。
- [x] 标签创建、选择、删除。
- [x] 图片和文档附件加入草稿。
- [x] 右侧抽屉带打开/关闭动画。
- [x] 设置页包含可替换数据源选择器、Notion 引导、系统设置（开机启动 + 快捷键录制）、界面、标签管理。
- [x] Notion 真实连接、schema 映射、分页读取、远程直连读写、本地模式拉取/推送、归档和附件上传；附件复用 file upload 引用，编辑时不重复上传。
- [x] 日历总览刷新按钮、本地数据源仅显示已绑定数据集。
- [x] Tauri 托盘、关闭隐藏、桌面快捷键桥接。
- [x] GitHub Actions 覆盖 Windows、macOS、Android 构建入口。
